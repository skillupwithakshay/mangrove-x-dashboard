# Extra deps for the LinkedIn tooling only (kept out of the main
# requirements.txt so the other pipelines stay lightweight).
#
# openpyxl: read the Page admin Excel exports in build_linkedin_from_export.py
#           (the current LinkedIn source — the export path).
# selenium: only needed if you ever run the optional LINKEDIN_SOURCE=selenium
#           scraper (not used now that LinkedIn is export-driven).
openpyxl>=3.1
selenium>=4.15
